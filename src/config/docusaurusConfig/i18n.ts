import type { I18nConfig } from "@docusaurus/types";
import { DeepPartial } from 'utility-types';

const config: DeepPartial<I18nConfig> = {
  defaultLocale: 'en',
  locales: ['en'],
};

export default config;
